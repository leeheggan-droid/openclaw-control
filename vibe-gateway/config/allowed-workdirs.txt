# allowed-workdirs.txt — allowlist of host directories that may be bind-mounted
# into the vibe container.  One absolute path per line.  Blank lines and lines
# starting with '#' are ignored.
#
# Installed to: /opt/openclaw/gateway/config/allowed-workdirs.txt
#
# Only paths listed here (or paths whose prefix matches a listed entry) will
# be accepted by forced-command.sh.  Add every repo / workspace that vibe
# should be permitted to touch.

/srv/openclaw-work
/opt/openclaw-crypto
